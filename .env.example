# Optional AI vision features for future versions (not used in MVP)
OPENAI_API_KEY=

# Optional backend for cloud sync (not used in MVP — app uses localStorage)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional Stripe payment features (not used in MVP)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# STRIPE_SECRET_KEY=
